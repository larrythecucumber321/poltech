import { Card, CardContent, CardHeader, CardTitle } from "@/components/card";

export default function DiscoverPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Discover</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Discover new friends and clubs here.</p>
      </CardContent>
    </Card>
  );
}
